/**
 * @file Enums - Modifier
 * @module docast/enums/Modifier
 */

/**
 * Modifier keywords.
 *
 * @enum {Lowercase<string>}
 */
enum Modifier {
  ABSTRACT = 'abstract',
  ASYNC = 'async',
  DECLARE = 'declare',
  DEFAULT = 'default',
  EXPORT = 'export',
  OVERRIDE = 'override',
  PRIVATE = 'private',
  PROTECTED = 'protected',
  PUBLIC = 'public',
  READONLY = 'readonly',
  STATIC = 'static'
}

export default Modifier
